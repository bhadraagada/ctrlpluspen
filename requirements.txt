# Handwriting Detection & OCR Pipeline
# RTX 3050 Optimized (8GB VRAM)
# Install with: uv pip install -r requirements.txt

# PyTorch with CUDA 11.8 (install separately first)
# uv pip install torch==2.1.0 torchvision==0.16.0 torchaudio==2.1.0 --index-url https://download.pytorch.org/whl/cu118

# Core ML/DL
ultralytics==8.0.200
transformers==4.35.0
accelerate==0.24.0

# OCR alternatives
easyocr==1.7.1

# Image processing
opencv-python==4.8.1.78
Pillow==10.1.0
albumentations==1.3.1

# Data & utilities
numpy==1.24.3
pandas==2.1.1
scipy==1.11.3
scikit-learn==1.3.2

# Evaluation metrics
editdistance==0.6.2
jiwer==3.0.3
pycocotools-windows==2.0.0.2; sys_platform == 'win32'
pycocotools==2.0.7; sys_platform != 'win32'

# Visualization
matplotlib==3.8.0
seaborn==0.13.0

# Config & CLI
pyyaml==6.0.1
click==8.1.7
tqdm==4.66.1
rich==13.6.0

# Logging
tensorboard==2.15.0
